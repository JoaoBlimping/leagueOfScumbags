module Scumbag
{
  /** characters in the way that they get saved to file and between battles and
   * stuff where they are actually real */
  export interface Character
  {
    key:string;
    level:number;
    health:number;
  }
}
